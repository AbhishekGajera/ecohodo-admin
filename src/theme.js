import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ffffff', // White color for the button background
      contrastText: '#000000', // Black color for the button text
    },
  },
});

export default theme;
  